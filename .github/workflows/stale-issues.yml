name: Close Stale Issues

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  stale:
    if: github.repository == 'MikeSiLVO/script.skin.info.service'
    permissions:
      issues: write
    uses: MikeSiLVO/workflows/.github/workflows/stale-issues.yml@v2.0.0
    secrets: inherit
