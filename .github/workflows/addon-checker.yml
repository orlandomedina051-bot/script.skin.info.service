name: Kodi addon-checker

on:
  push:
    branches: [main]
    paths-ignore: ['**/*.po']
  pull_request:
    branches: [main]
    paths-ignore: ['**/*.po']

jobs:
  addon-checker:
    if: github.repository == 'MikeSiLVO/script.skin.info.service'
    uses: MikeSiLVO/workflows/.github/workflows/addon-checker.yml@v2.0.0
    with:
      kodi_branch: omega
